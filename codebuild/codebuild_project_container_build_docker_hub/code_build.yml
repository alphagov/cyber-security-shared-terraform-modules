version: 0.2

env:
  shell: bash

phases:
  pre_build:
    commands:
      - docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_PASSWORD
      - ls $CODEBUILD_SRC_DIR_git_diff_file
      - REBUILD_FILE="${CODEBUILD_SRC_DIR_git_diff_file}/rebuild_task.json"
      - if [ -f "$REBUILD_FILE" ]; then
            BUILD=$(cat $REBUILD_FILE);
        else
            echo Rebuild file not found, defaulting to true...;
            BUILD=true;
        fi

  build:
    commands:
      - if $BUILD; then
          echo Build started on `date`;
          echo Building the Docker image...;
        else
          echo Build set to false, not rebuilding image;
        fi
  post_build:
    commands:
      - if $BUILD; then
          echo Build completed on `date`;
          echo Pushing the Docker image...;
        else
          echo Build set to false, not pushing image;
        fi