version: 0.2

env:
  shell: bash

phases:
  build:
    commands:
      - echo outputting git diff file...
      - cd $CODEBUILD_SRC_DIR
      - test="${GITHUB_PAT:0:2}" ; echo "${test}"
      - source /usr/local/bin/get_pr_changed_files_list.sh ${REPO_NAME} ${OUTPUT_FILENAME}

artifacts:
  secondary-artifacts:
    git-diff-file:
      base-directory: $CODEBUILD_SRC_DIR
      files: 
        - ${OUTPUT_FILENAME}
